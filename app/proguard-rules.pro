# Release builds do not minify (see app/build.gradle.kts), so this file is a placeholder.
# Should minification ever be turned on, the WebView JavaScript bridge would need keeping —
# there is none today, and adding one to a never-patched Chromium 69 would be a poor idea.
