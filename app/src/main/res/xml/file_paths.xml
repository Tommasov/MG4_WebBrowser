<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The app-private external dir downloads land in, so a fetched APK can be shared with
         the system package installer via a content:// URI. Must match ApkDownloader.SUBDIR. -->
    <external-files-path name="downloads" path="downloads/" />
</paths>
